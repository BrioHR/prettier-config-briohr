# Prettier config BrioHR
<hr>
Prettier config for the teams working on BrioHR.

# Install
<hr>

```
npm i -D @briohr/prettier-config-briohr prettier
```

Add it as a config in ```package.json```
```json
{
  "prettier": "@briohr/prettier-config-briohr"
}
```
